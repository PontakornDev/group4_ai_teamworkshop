---
phase: 02-login-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
autonomous: true
requirements:
  - LOGIN-02
must_haves:
  truths:
    - "Quicksand font is imported from Google Fonts and set as the default sans-serif"
    - "Material Symbols Outlined icon font is imported from Google Fonts"
    - "Full design token @theme block exists with all required color, spacing, and typography tokens"
    - "SessionProvider from next-auth/react wraps all children in the root layout"
    - "Root layout has Pawnder title and description metadata"
    - "Body element applies font-sans class mapping to Quicksand"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind v4 @theme design tokens + Google Fonts imports"
      contains: "@theme"
    - path: "app/layout.tsx"
      provides: "Root layout with SessionProvider and Pawnder metadata"
      exports: ["RootLayout"]
  key_links:
    - from: "app/layout.tsx"
      to: "next-auth/react SessionProvider"
      via: "import + JSX wrapper"
      pattern: "SessionProvider"
    - from: "app/globals.css"
      to: "Google Fonts CDN"
      via: "@import url"
      pattern: "fonts.googleapis.com.*Quicksand"
---

<objective>
Verify that globals.css and layout.tsx are correctly implemented: design tokens loaded, Quicksand font wired, SessionProvider wrapping the app, and Pawnder metadata in place.

Purpose: These two files are the foundation for all UI — wrong tokens or a missing SessionProvider breaks every subsequent phase.
Output: Confirmation that globals.css and layout.tsx satisfy their acceptance criteria with no changes needed (or targeted fixes if gaps are found).

Note: This plan verifies completed work. MVP vertical-slice structure does not apply — there is no new feature being built.
</objective>

<execution_context>
@/Users/c.ptk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/c.ptk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-login-page/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify globals.css — design tokens and font imports</name>
  <files>app/globals.css</files>

  <read_first>
    - app/globals.css — the file under verification
    - .planning/phases/02-login-page/02-CONTEXT.md — design token requirements
    - CLAUDE.md (Design System section) — canonical token values
  </read_first>

  <action>
Read app/globals.css and verify each of the following in order. If any criterion fails, apply the minimal targeted fix.

1. Quicksand font @import appears before `@import "tailwindcss"` (Tailwind v4 requires CSS @imports first)
2. Material Symbols Outlined @import present before `@import "tailwindcss"`
3. `@import "tailwindcss"` is present
4. `@theme {` block is present
5. Core color tokens present: `--color-primary: #9b4500`, `--color-on-primary: #ffffff`, `--color-primary-container: #ff8c42`, `--color-surface: #fbf9f8`, `--color-error: #ba1a1a`, `--color-tertiary-container: #83ba48`, `--color-outline-variant: #ddc1b3`, `--color-on-surface: #1b1c1c`, `--color-on-surface-variant: #564338`
6. `--font-sans` set to `"Quicksand", sans-serif`
7. Spacing tokens present: `--spacing-xs: 4px`, `--spacing-sm: 12px`, `--spacing-md: 24px`, `--spacing-container-padding: 20px`
8. Typography size tokens present: `--text-display: 40px`, `--text-headline-lg: 32px`
9. `@layer utilities` block with `.text-display { font-weight: 700; }` is present
10. `.material-symbols-outlined` CSS class with `font-variation-settings` is present
  </action>

  <verify>
    <automated>
grep -F "fonts.googleapis.com/css2?family=Quicksand" app/globals.css &&
grep -F "Material+Symbols+Outlined" app/globals.css &&
grep -F '@import "tailwindcss"' app/globals.css &&
grep -F '@theme {' app/globals.css &&
grep -F '--color-primary: #9b4500' app/globals.css &&
grep -F '--color-error: #ba1a1a' app/globals.css &&
grep -F '--color-tertiary-container: #83ba48' app/globals.css &&
grep -F '--font-sans: "Quicksand"' app/globals.css &&
grep -F '--spacing-md: 24px' app/globals.css &&
grep -F '--text-display: 40px' app/globals.css &&
grep -F '.material-symbols-outlined' app/globals.css &&
echo "globals.css: ALL CHECKS PASSED"
    </automated>
  </verify>

  <acceptance_criteria>
    - `app/globals.css` contains `fonts.googleapis.com/css2?family=Quicksand`
    - `app/globals.css` contains `Material+Symbols+Outlined`
    - `app/globals.css` contains `@import "tailwindcss"`
    - `app/globals.css` contains `@theme {`
    - `app/globals.css` contains `--color-primary: #9b4500`
    - `app/globals.css` contains `--color-error: #ba1a1a`
    - `app/globals.css` contains `--color-tertiary-container: #83ba48`
    - `app/globals.css` contains `--font-sans: "Quicksand"`
    - `app/globals.css` contains `--spacing-md: 24px`
    - `app/globals.css` contains `--text-display: 40px`
    - `app/globals.css` contains `.material-symbols-outlined`
    - All Google Fonts @import lines appear BEFORE `@import "tailwindcss"` in the file
  </acceptance_criteria>

  <done>All grep commands above exit 0. globals.css fully satisfies design token and font requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Verify layout.tsx — SessionProvider, metadata, font-sans body class</name>
  <files>app/layout.tsx</files>

  <read_first>
    - app/layout.tsx — the file under verification
    - .planning/phases/02-login-page/02-CONTEXT.md — D-07 (SessionProvider directly in layout, no providers.tsx wrapper)
  </read_first>

  <action>
Read app/layout.tsx and verify each of the following. If any criterion fails, apply the minimal targeted fix.

1. `import { SessionProvider } from "next-auth/react"` is present (per D-07 — no providers.tsx wrapper)
2. `export const metadata` block has `title: "Pawnder"` and a description field
3. The JSX body wraps `{children}` inside `<SessionProvider>` — not a sibling, but a parent wrapper
4. The `<body>` element has `font-sans` class (maps to Quicksand via --font-sans token in globals.css)
5. No separate `providers.tsx` file is needed or imported (v5 pattern — SessionProvider goes directly here per D-07)
  </action>

  <verify>
    <automated>
grep -F 'SessionProvider' app/layout.tsx &&
grep -F '"Pawnder"' app/layout.tsx &&
grep -F 'font-sans' app/layout.tsx &&
grep -F 'next-auth/react' app/layout.tsx &&
echo "layout.tsx: ALL CHECKS PASSED"
    </automated>
  </verify>

  <acceptance_criteria>
    - `app/layout.tsx` contains `SessionProvider` (imported and used as JSX wrapper)
    - `app/layout.tsx` contains `"next-auth/react"` as import source
    - `app/layout.tsx` contains `"Pawnder"` in the metadata block
    - `app/layout.tsx` contains `font-sans` on the `<body>` element
    - `{children}` appears inside `<SessionProvider>` tags (not outside)
  </acceptance_criteria>

  <done>All grep commands above exit 0. layout.tsx has SessionProvider wrapping children, Pawnder metadata, and font-sans on body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS @theme → browser | Design tokens are inert CSS variables; no trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02a-01 | Information Disclosure | globals.css | accept | CSS is public by design; tokens contain no secrets — hex colors and font names only |
| T-02a-02 | Tampering | app/layout.tsx SessionProvider | accept | SessionProvider is a React context provider; session token validation happens in auth.ts, not here |
</threat_model>

<verification>
Run both automated verify blocks in sequence. Both must exit 0 with no grep failures before this plan is considered complete.

```bash
# Full combined check
grep -F "fonts.googleapis.com/css2?family=Quicksand" app/globals.css
grep -F '@theme {' app/globals.css
grep -F '--color-primary: #9b4500' app/globals.css
grep -F '--font-sans: "Quicksand"' app/globals.css
grep -F 'SessionProvider' app/layout.tsx
grep -F '"Pawnder"' app/layout.tsx
grep -F 'font-sans' app/layout.tsx
echo "Phase 02 Plan 01: VERIFIED"
```
</verification>

<success_criteria>
- globals.css has Quicksand + Material Symbols imports before Tailwind import
- globals.css @theme block contains all required color, spacing, and typography tokens
- layout.tsx imports and uses SessionProvider from next-auth/react (D-07 — directly, no wrapper file)
- layout.tsx metadata has title "Pawnder"
- body element applies font-sans class

Note: Live OAuth end-to-end testing (D-09) is a post-verification step performed by the developer with real Google credentials — not covered by this automated plan.
</success_criteria>

<output>
After completion, create `.planning/phases/02-login-page/02-01-SUMMARY.md`
</output>
