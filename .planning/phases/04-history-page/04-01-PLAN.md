# Phase 4 Plan 01: Extend HistoryList with filter, sort, and summary bar

**Phase:** 4 — History Page
**Goal:** Add client-side user-filtering, action filter pills, sort dropdown, sticky summary bar, filtered empty state, and typography corrections to `HistoryList.tsx`.
**Files changed:** `components/HistoryList.tsx`
**Depends on:** none

**Design deviation note (D-06 / desktop SortDropdown):** Per UI-SPEC desktop layout, the SortDropdown belongs in the page header (right of title). That requires lifting `sort` state to the server component — adding props and wiring that would span both files with minimal UX gain. Per D-09 (filter and sort state in `useState` inside `HistoryList`), the SortDropdown remains inside `HistoryList` for both breakpoints. On desktop it sits in the filter row, pushed right via `ml-auto`. This is an intentional deviation from the UI-SPEC desktop header placement; D-09 takes precedence.

**D-06 stats scope note:** Summary bar stats are computed from `mine` (all user records, pre-filter) — not from `filtered` (post-filter). This gives a permanent totals overview in the summary bar. The filter pills narrow only the card list. This interprets "computed from the current user's filtered records" in D-06/D-12 as "filtered to the current user" (not "filtered by active action pill"). This is deliberate for better UX: the bar shows totals at a glance regardless of active filter.

---

## Tasks

### Task 1: Add imports, hooks, and user-filter + sort logic

**File:** `components/HistoryList.tsx`

**Change:** Update the `"react"` import and insert derived-data logic into the `HistoryList` function body. This task is **logic-only** — do not modify the `ActionBadge` component, the `ColEntry`/`DogRecord`/`HistoryListProps` interfaces, or the JSX return block. Task 2 replaces the entire return block and is the authoritative source for all JSX.

**Step 1 — Update import at top of file:**

Before:
```tsx
import { useState, useMemo } from "react";
```

The file currently has no React import (it uses only `"use client"` and `Image`). Add this import after the `"use client"` directive:

```tsx
import { useState, useMemo } from "react";
```

**Step 2 — Insert hooks and derived data inside `HistoryList` function body.**

Insert the following block as the **first lines** inside `HistoryList` (before the existing `if (records.length === 0)` guard). Do not modify anything else in the function body — Task 2 will replace the full return.

```tsx
const [filter, setFilter] = useState<"all" | "like" | "dislike">("all");
const [sort, setSort] = useState<"newest" | "oldest">("newest");

// Guard: currentUser undefined means we cannot match swipes to a user — show empty state
// (Placed after hooks to satisfy Rules of Hooks; useMemos below still run but produce [])
const mine = useMemo(
  () =>
    currentUser
      ? records.filter((r) => r.col.some((e) => e.username === currentUser))
      : [],
  [records, currentUser]
);

// Apply action filter — runs after user-scope filter (D-07, D-09)
const filtered = useMemo(() => {
  if (filter === "all") return mine;
  return mine.filter(
    (r) => r.col.find((e) => e.username === currentUser)?.action === filter
  );
}, [mine, filter, currentUser]);

// Sort by current user's swipe timestamp (D-08) — applied after filter
const sorted = useMemo(() => {
  return [...filtered].sort((a, b) => {
    const tsA = a.col.find((e) => e.username === currentUser)?.timestamp ?? "";
    const tsB = b.col.find((e) => e.username === currentUser)?.timestamp ?? "";
    return sort === "newest"
      ? tsB.localeCompare(tsA)
      : tsA.localeCompare(tsB);
  });
}, [filtered, sort, currentUser]);

// Summary stats — computed from mine (pre-filter totals) not filtered (D-05, D-06 per plan deviation note)
const stats = useMemo(() => {
  const likes = mine.filter(
    (r) => r.col.find((e) => e.username === currentUser)?.action === "like"
  ).length;
  const dislikes = mine.filter(
    (r) => r.col.find((e) => e.username === currentUser)?.action === "dislike"
  ).length;
  return { total: mine.length, likes, dislikes };
}, [mine, currentUser]);
```

**No other changes in Task 1.** Do not touch the existing `if (records.length === 0)` guard, the `return (<>...</>)` block, or the `.map()` calls — Task 2 replaces all of that.

---

### Task 2: Replace return block with SummaryBar, FilterPills, SortDropdown, and filtered empty state

**File:** `components/HistoryList.tsx`

**Change:** Replace the **entire content of the `HistoryList` function from `if (records.length === 0)` to the end of the function** (i.e., from the global empty guard through the final closing `}`). Keep everything above — the interfaces, `ActionBadge`, and Task 1's inserted hooks/logic — unchanged.

Also update `ActionBadge` typography while in this file: change `font-label-sm text-label-sm` → `font-bold text-label-lg` in both the `like` and `dislike` spans inside `ActionBadge`, and update the desktop card timestamp class the same way. This aligns with UI-SPEC typography (label sizes corrected from 12px label-sm to 14px label-lg).

**ActionBadge update (2 spots inside existing `ActionBadge` component):**

Before (like span):
```tsx
<span className="inline-flex items-center gap-xs px-sm py-xs rounded-full bg-tertiary-container text-on-tertiary-container font-label-sm text-label-sm">
```
After:
```tsx
<span className="inline-flex items-center gap-xs px-sm py-xs rounded-full bg-tertiary-container text-on-tertiary-container font-bold text-label-lg">
```

Before (dislike span):
```tsx
<span className="inline-flex items-center gap-xs px-sm py-xs rounded-full bg-error-container text-on-error-container font-label-sm text-label-sm">
```
After:
```tsx
<span className="inline-flex items-center gap-xs px-sm py-xs rounded-full bg-error-container text-on-error-container font-bold text-label-lg">
```

**Replace the rest of the function body (from `if (records.length === 0)` to end) with:**

```tsx
  if (mine.length === 0) {
    return (
      <div className="flex flex-col items-center justify-center py-xl text-on-surface-variant">
        <span className="material-symbols-outlined text-display mb-md" style={{ fontVariationSettings: "'FILL' 0" }}>pets</span>
        <p className="font-headline-md text-headline-md">No swipes yet</p>
        <p className="font-body-md text-body-md mt-xs">Start swiping to see your history here.</p>
      </div>
    );
  }

  return (
    <>
      {/* SummaryBar — sticky below mobile top bar, non-sticky on desktop (D-04, D-05) */}
      <div className="sticky top-16 md:top-0 z-30 bg-surface py-sm flex flex-wrap gap-sm mb-sm">
        <span className="inline-flex items-center gap-xs px-sm py-xs rounded-full bg-surface-container-low border border-outline-variant text-on-surface font-bold text-label-lg">
          {stats.total} Swiped
        </span>
        <span className="inline-flex items-center gap-xs px-sm py-xs rounded-full bg-surface-container-low border border-outline-variant text-on-surface font-bold text-label-lg">
          ❤ {stats.likes} Likes
        </span>
        <span className="inline-flex items-center gap-xs px-sm py-xs rounded-full bg-surface-container-low border border-outline-variant text-on-surface font-bold text-label-lg">
          ✕ {stats.dislikes} Dislikes
        </span>
      </div>

      {/* FilterPills + SortDropdown row (D-07, D-08, D-09) */}
      <div className="flex flex-wrap items-center gap-sm mb-md">
        <div className="flex gap-xs overflow-x-auto">
          {(["all", "like", "dislike"] as const).map((f) => {
            const label = f === "all" ? "All" : f === "like" ? "Likes" : "Dislikes";
            const active = filter === f;
            return (
              <button
                key={f}
                onClick={() => setFilter(f)}
                className={
                  active
                    ? "bg-primary-container text-on-primary-container font-bold text-label-lg px-6 py-2 rounded-full shadow-sm"
                    : "bg-surface-container-low text-on-surface-variant font-bold text-label-lg px-6 py-2 rounded-full border border-outline-variant hover:bg-surface-container-high transition-colors"
                }
              >
                {label}
              </button>
            );
          })}
        </div>
        <select
          value={sort}
          onChange={(e) => setSort(e.target.value as "newest" | "oldest")}
          className="ml-auto bg-surface-container-low border border-outline-variant rounded-full px-sm py-xs font-normal text-body-md text-on-surface focus:outline-none focus:ring-1 focus:ring-primary"
        >
          <option value="newest">Newest first</option>
          <option value="oldest">Oldest first</option>
        </select>
      </div>

      {/* Filtered empty state (D-07) */}
      {sorted.length === 0 && (
        <div className="flex flex-col items-center py-xl text-on-surface-variant">
          <span
            className="material-symbols-outlined mb-md"
            style={{ fontSize: 48, fontVariationSettings: "'FILL' 0" }}
          >
            filter_list
          </span>
          <p className="font-headline-md text-headline-md">
            No {filter === "like" ? "Likes" : "Dislikes"} found
          </p>
          <p className="font-body-md text-body-md mt-xs">
            Try switching to All to see your full swipe history.
          </p>
        </div>
      )}

      {/* Mobile list */}
      <div className="md:hidden flex flex-col gap-sm">
        {sorted.map((dog) => {
          const myEntry = dog.col.find((e) => e.username === currentUser) ?? dog.col[0];
          return (
            <div
              key={dog.dogId}
              className="bg-surface-container-lowest rounded-xl p-sm shadow-sm flex items-center gap-md hover:shadow-md transition-shadow"
            >
              <div className="relative w-16 h-16 flex-shrink-0">
                <Image
                  src={dog.imageUrl}
                  alt={`Dog ${dog.dogId.slice(0, 8)}`}
                  fill
                  className="object-cover rounded-full border-2 border-primary-fixed-dim"
                  unoptimized
                />
              </div>
              <div className="flex-1 min-w-0">
                <h3 className="font-headline-md text-headline-md text-on-surface truncate">
                  #{dog.dogId.slice(0, 8)}
                </h3>
                <p className="font-body-md text-body-md text-on-surface-variant">
                  {dog.col.length} swipe{dog.col.length !== 1 ? "s" : ""}
                </p>
              </div>
              {myEntry && <ActionBadge action={myEntry.action} />}
            </div>
          );
        })}
      </div>

      {/* Desktop grid */}
      <div className="hidden md:grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-md">
        {sorted.map((dog) => {
          const myEntry = dog.col.find((e) => e.username === currentUser) ?? dog.col[0];
          return (
            <div
              key={dog.dogId}
              className="bg-surface-container-lowest rounded-[24px] p-md flex flex-col items-center text-center shadow-[0_4px_24px_rgba(0,0,0,0.04)] hover:shadow-[0_8px_16px_rgba(0,0,0,0.08)] hover:-translate-y-1 transition-all duration-300 border border-surface-variant"
            >
              <div className="relative w-32 h-32 mb-sm rounded-full overflow-hidden border-4 border-surface flex-shrink-0">
                <Image
                  src={dog.imageUrl}
                  alt={`Dog ${dog.dogId.slice(0, 8)}`}
                  fill
                  className="object-cover"
                  unoptimized
                />
              </div>
              <h3 className="font-headline-md text-headline-md text-on-surface mb-xs">
                #{dog.dogId.slice(0, 8)}
              </h3>
              <p className="font-body-md text-body-md text-on-surface-variant mb-md">
                {dog.col.length} swipe{dog.col.length !== 1 ? "s" : ""}
              </p>
              {myEntry && (
                <div className="mt-auto">
                  <ActionBadge action={myEntry.action} />
                  {myEntry.timestamp && (
                    <p className="font-bold text-label-lg text-on-surface-variant mt-xs">
                      {new Date(myEntry.timestamp).toLocaleDateString()}
                    </p>
                  )}
                </div>
              )}
            </div>
          );
        })}
      </div>
    </>
  );
```

---

## Verification

- [ ] `/history` page loads without TypeScript or runtime errors in the browser console
- [ ] Visiting `/history` as a logged-in user shows only dogs that user personally swiped — dogs swiped only by other users do not appear
- [ ] SummaryBar is visible and sticky — shows three chips: "[N] Swiped", "❤ [N] Likes", "✕ [N] Dislikes" where all three numbers match the current user's actual swipe data
- [ ] Summary bar chip counts do NOT change when a filter pill is active (they always show totals)
- [ ] Clicking "Likes" filter pill shows only like-action cards; clicking "Dislikes" shows only dislike cards; clicking "All" restores the full list
- [ ] SortDropdown defaults to "Newest first"; switching to "Oldest first" reverses the card order based on the current user's swipe timestamp
- [ ] When the filter returns zero results, the filtered empty state renders with `filter_list` icon, heading "No Likes found" or "No Dislikes found", and body "Try switching to All to see your full swipe history."
- [ ] When the user has zero swipes total, the global empty state renders: "No swipes yet" with the pets icon
- [ ] ActionBadge uses `font-bold text-label-lg` (not `text-label-sm`) — verify by inspecting badge element in DevTools
- [ ] Desktop card timestamp uses `font-bold text-label-lg` (not `text-label-sm`)
- [ ] `npx tsc --noEmit` exits 0 (no TypeScript errors)
