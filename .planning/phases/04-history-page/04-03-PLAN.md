---
phase: 04-history-page
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04

must_haves:
  truths:
    - "ROADMAP Phase 4 SC-1 says records are grouped by dog (not by username)"
    - "ROADMAP Phase 4 SC-2 says summary bar shows the current user's total likes and dislikes (not per user)"
    - "HIST-02 in REQUIREMENTS.md reflects current-user-only display matching D-02 and D-03"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 success criteria with correct grouping and summary scope wording"
      contains: "grouped by dog"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HIST-02 requirement matching what was built per user decisions"
      contains: "current user's action badge"
  key_links:
    - from: "ROADMAP.md SC-1"
      to: "HistoryList.tsx dog-grouped cards"
      via: "consistent wording"
      pattern: "grouped by dog"
    - from: "REQUIREMENTS.md HIST-02"
      to: "D-02 and D-03 in CONTEXT.md"
      via: "requirement text matches user decision"
      pattern: "current user"
---

<objective>
Reconcile ROADMAP.md and REQUIREMENTS.md wording with the user's architectural decisions captured in CONTEXT.md.

The implementation correctly groups history records by dog and shows the current user's action only — matching D-01, D-02, D-03, D-05, and D-06 from CONTEXT.md. However, the ROADMAP success criteria and HIST-02 requirement were never updated after those decisions were made. This plan makes four surgical text edits so that the contracts match the code and re-verification passes.

Purpose: Close Gap 1 and Gap 2 from VERIFICATION.md. No code changes — doc-only.
Output: Updated ROADMAP.md (overview bullet + Goal + SC-1 + SC-2 reworded) and updated REQUIREMENTS.md (HIST-02 reworded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-history-page/04-CONTEXT.md
@.planning/phases/04-history-page/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP Phase 4 success criteria SC-1 and SC-2</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    Read .planning/ROADMAP.md lines 21-26 (v1.0 overview checklist) and lines 65-82 (Phase 4 Details block). The current text contains "grouped by username" in three places:
    - Overview bullet (~line 25): "- [ ] **Phase 4: History Page** - Show all like/dislike records grouped by username with summary bar, filters, and sort"
    - Goal line (~line 66): "**Goal**: Users can view all swipe records grouped by username — with a summary bar showing total likes/dislikes per user, action filter (all/like/dislike), and timestamp sort"
    - SC-1 (~line 71): "  1. /history page lists all swipe records from swipes.json, grouped by username"
    - SC-2 (~line 72): "  2. Summary bar at top shows total likes count and total dislikes count per user"

    Also read .planning/phases/04-history-page/04-CONTEXT.md decisions D-01 through D-06 for the correct intent.
  </read_first>
  <action>
    Make four targeted line replacements in ROADMAP.md. Do not touch any other lines.

    Replace the v1.0 overview checklist bullet (~line 25):
      OLD: "- [ ] **Phase 4: History Page** - Show all like/dislike records grouped by username with summary bar, filters, and sort"
      NEW: "- [ ] **Phase 4: History Page** - Show swipe records grouped by dog with summary bar, filters, and sort"

    Replace the Phase 4 goal line (~line 66) which still says "grouped by username":
      OLD: "**Goal**: Users can view all swipe records grouped by username — with a summary bar showing total likes/dislikes per user, action filter (all/like/dislike), and timestamp sort"
      NEW: "**Goal**: Users can view their own swipe records grouped by dog — with a summary bar showing their total likes/dislikes, action filter (all/like/dislike), and timestamp sort"

    Replace SC-1 (~line 71):
      OLD: "  1. /history page lists all swipe records from swipes.json, grouped by username"
      NEW: "  1. /history page lists the current user's swipe records from swipes.json, grouped by dog"

    Replace SC-2 (~line 72):
      OLD: "  2. Summary bar at top shows total likes count and total dislikes count per user"
      NEW: "  2. Summary bar at top shows the current user's total likes count and total dislikes count"

    Use the Edit tool for surgical replacements — do NOT rewrite the file.
  </action>
  <verify>
    <automated>grep -n "grouped by dog" /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md</automated>
    Expected: at least 3 lines matching (overview bullet + Goal line + SC-1 line).
    <automated>grep -c "grouped by username" /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md</automated>
    Expected: 0 (no remaining occurrences anywhere in ROADMAP.md).
    <automated>grep -n "current user" /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md</automated>
    Expected: SC-2 line present with "current user's total likes count".
  </verify>
  <done>
    ROADMAP.md overview bullet, Phase 4 Goal, SC-1, and SC-2 all reflect dog-grouped, current-user-only wording. Zero occurrences of "grouped by username" remain anywhere in the file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix HIST-02 in REQUIREMENTS.md to match D-02 and D-03</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    Read .planning/REQUIREMENTS.md lines 36-40. Current HIST-02 text:
      "HIST-02: History page shows each dog record's dogId, imageUrl thumbnail, and all swipes from its col array (username, email, action, timestamp per entry)"

    The user decided (D-02, D-03) to show only the current user's action badge plus total swipe count — not iterate all col entries. Email is not displayed. This requirement was never reconciled with those decisions.
  </read_first>
  <action>
    Replace the HIST-02 line in REQUIREMENTS.md. Use the Edit tool for a surgical single-line replacement.

    OLD:
      "- **HIST-02**: History page shows each dog record's dogId, imageUrl thumbnail, and all swipes from its `col` array (username, email, action, timestamp per entry)"

    NEW:
      "- **HIST-02**: History page shows each dog card with a truncated dogId label, imageUrl thumbnail, the current user's action badge (like/dislike), swipe timestamp, and total swipe count across all users"

    Do not touch any other line in REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -n "HIST-02" /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/REQUIREMENTS.md</automated>
    Expected: single line containing "current user's action badge" and "swipe timestamp".
    <automated>grep -c "col array" /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/REQUIREMENTS.md</automated>
    Expected: 0 occurrences of the old col-array language in HIST-02 (SWIPE-01's col array reference in a different line is fine — confirm the HIST-02 line itself has no "col array").
  </verify>
  <done>
    HIST-02 in REQUIREMENTS.md reads: "History page shows each dog card with a truncated dogId label, imageUrl thumbnail, the current user's action badge (like/dislike), swipe timestamp, and total swipe count across all users". No "col array" or "username, email, action, timestamp per entry" language remains in HIST-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File edit | No user data involved; pure doc update, no security surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | ROADMAP.md edits | accept | Doc-only changes; version-controlled in git; no runtime impact |
</threat_model>

<verification>
After both tasks complete:
1. `grep -n "grouped by dog" .planning/ROADMAP.md` — returns overview bullet + Goal line + SC-1 line (3 matches)
2. `grep -c "grouped by username" .planning/ROADMAP.md` — returns 0
3. `grep -n "current user" .planning/ROADMAP.md` — returns SC-2 line
4. `grep -n "HIST-02" .planning/REQUIREMENTS.md` — shows new text with "current user's action badge" and "swipe timestamp"
5. `grep -c "col array" .planning/REQUIREMENTS.md` — HIST-02 line no longer contains "col array" (SWIPE-01 still may — that is correct)
</verification>

<success_criteria>
- ROADMAP overview bullet, Phase 4 Goal, and SC-1 all say "grouped by dog"
- ROADMAP Phase 4 SC-2 says "current user's total likes count and total dislikes count"
- Zero occurrences of "grouped by username" remain anywhere in ROADMAP.md
- HIST-02 in REQUIREMENTS.md describes the actual per-card display: truncated dogId, thumbnail, current user's action badge, swipe timestamp, total swipe count
- No re-verification failures from doc/code mismatches on these two gaps
</success_criteria>

<output>
After completion, create `.planning/phases/04-history-page/04-03-SUMMARY.md` using the summary template.
</output>
