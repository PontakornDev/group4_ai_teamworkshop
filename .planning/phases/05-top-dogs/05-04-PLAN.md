---
phase: 05-top-dogs
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-03"
files_modified:
  - app/history/page.tsx
autonomous: true
requirements:
  - TOP-01
  - TOP-02
  - TOP-03
  - TOP-04

must_haves:
  truths:
    - "The /history page renders TopDogsSection above HistoryList"
    - "TopDogsSection receives topDogs prop from a server-side getTopDogs() call"
    - "The existing HistoryList and Navbar remain unchanged and functional"
    - "A real user visiting /history sees 'Top Dogs' section before the swipe history list"
  artifacts:
    - path: "app/history/page.tsx"
      provides: "History page with TopDogsSection wired above HistoryList"
      contains: "TopDogsSection"
  key_links:
    - from: "app/history/page.tsx"
      to: "lib/storage.ts getTopDogs"
      via: "direct server-side import and await"
      pattern: "getTopDogs\\(\\)"
    - from: "app/history/page.tsx"
      to: "components/TopDogsSection.tsx"
      via: "JSX insertion above HistoryList"
      pattern: "<TopDogsSection"
---

## Phase Goal

**As a** dog-loving user, **I want to** see which dogs got the most likes and dislikes, **so that** I know which pups are crowd favorites and which are underdogs.

<objective>
Wire `TopDogsSection` into `app/history/page.tsx`. This is the integration step that makes the feature visible to users — after this plan, a real user visiting `/history` sees the Top Dogs leaderboard above their swipe history.

Purpose: Connects all prior slices (storage function, API route, UI component) into the live history page.
Output: Modified `app/history/page.tsx` that calls `getTopDogs()` server-side and passes the result to `<TopDogsSection />`.
</objective>

<execution_context>
@/Users/k6b6/.claude/get-shit-done/workflows/execute-plan.md
@/Users/k6b6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/CLAUDE.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/05-top-dogs/05-UI-SPEC.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/05-top-dogs/05-01-SUMMARY.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/05-top-dogs/05-03-SUMMARY.md

<interfaces>
<!-- Current app/history/page.tsx structure -->
import { auth } from "@/auth";
import { redirect } from "next/navigation";
import Navbar from "@/components/Navbar";
import HistoryList from "@/components/HistoryList";
import { readDogs } from "@/lib/storage";

export default async function HistoryPage() {
  const session = await auth();
  if (!session?.user) redirect("/login");
  const records = await readDogs();
  return (
    <div className="bg-surface text-on-surface min-h-screen flex antialiased">
      <Navbar />
      <main className="flex-1 md:ml-64 pb-24 md:pb-0">
        {/* Mobile header */}
        <header className="md:hidden ...">...</header>
        <div className="max-w-7xl mx-auto px-container-padding md:px-xl pt-lg">
          <div className="flex flex-col md:flex-row ...">...</div>
          {/* TopDogsSection inserts HERE — above HistoryList */}
          <HistoryList records={records} currentUser={...} />
        </div>
      </main>
    </div>
  );
}

<!-- New imports needed -->
import TopDogsSection from "@/components/TopDogsSection";
import { readDogs, getTopDogs } from "@/lib/storage";
// (replace the existing readDogs-only import with a destructured import of both)

<!-- New data call (inside HistoryPage, alongside readDogs call) -->
const topDogs = await getTopDogs();
// Pass to: <TopDogsSection topDogs={topDogs} />

<!-- Insertion point per 05-UI-SPEC.md Layout Integration -->
// <TopDogsSection topDogs={topDogs} /> goes between the title block div and <HistoryList />
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TopDogsSection into app/history/page.tsx</name>
  <files>app/history/page.tsx</files>
  <action>
    Modify `app/history/page.tsx` with three targeted changes:

    1. Update the storage import — change:
       `import { readDogs } from "@/lib/storage";`
       to:
       `import { readDogs, getTopDogs } from "@/lib/storage";`

    2. Add a new import for TopDogsSection after the HistoryList import line:
       `import TopDogsSection from "@/components/TopDogsSection";`

    3. Inside `HistoryPage()`, after the `const records = await readDogs();` line, add:
       `const topDogs = await getTopDogs();`

    4. In the JSX, insert `<TopDogsSection topDogs={topDogs} />` between the closing `</div>` of the title block (the `flex flex-col md:flex-row` div) and the `<HistoryList .../>` line.

    The existing `<Navbar />`, mobile header, title block, and `<HistoryList />` must remain exactly as they are — do not alter their props, classes, or structure. Do not add "use client" to this file — it is and must remain a server component.

    The result matches the insertion point shown in 05-UI-SPEC.md Layout Integration section:
    ```
    <TopDogsSection topDogs={topDogs} />
    <HistoryList records={records} currentUser={...} />
    ```
  </action>
  <verify>
    <automated>cd /Users/k6b6/Desktop/claude/group4_ai_teamworkshop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `app/history/page.tsx` imports both `readDogs` and `getTopDogs` from `@/lib/storage`
    - `app/history/page.tsx` imports `TopDogsSection` from `@/components/TopDogsSection`
    - `<TopDogsSection topDogs={topDogs} />` appears in JSX above `<HistoryList />`
    - TypeScript compiles with no errors
    - All existing functionality (Navbar, auth guard, HistoryList) untouched
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All four Phase 5 slices are complete: getTopDogs() in storage, GET /api/stats, TopDogsSection component, and history page integration.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/history (must be signed in)
    2. Verify "Top Dogs" heading appears above the swipe history list
    3. If swipes.json has likes/dislikes: confirm two cards appear side-by-side — "Most Liked" with orange (#9b4500) label, "Most Disliked" with red (#ba1a1a) label
    4. Each card should show: dog image (140px tall), dogId truncated to 8 chars with # prefix, count badge (large number + " likes" / " dislikes")
    5. If swipes.json is empty: "No top dogs yet" heading and "No swipes yet — start swiping to see top dogs!" body should appear in the section
    6. Verify `curl http://localhost:3000/api/stats` returns JSON with mostLiked and mostDisliked keys (no auth required)
    7. Verify the history list below Top Dogs is unchanged
    8. Verify mobile view: section still shows, cards stack in 2-column grid
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual/functional issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server component → HTML output | topDogs data rendered into the page; imageUrl used as Next.js Image src |
| Browser → /history | Route is auth-guarded via existing session check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | /history page (server render) | accept | Top Dogs data is aggregate counts and dogIds only — no user-identifying data exposed in the section. Auth guard already in place for the page. |
| T-05-08 | Spoofing | imageUrl in TopDogsSection | accept | imageUrl originates from random.dog (project's existing trusted source). Next.js Image component handles rendering — no direct innerHTML injection. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors
- `grep -c "TopDogsSection" app/history/page.tsx` returns 2 (import + JSX usage)
- `grep -c "getTopDogs" app/history/page.tsx` returns 2 (import + call)
- `<TopDogsSection` appears before `<HistoryList` in file order
- Human checkpoint passed: Top Dogs section visible above swipe history on /history page
</verification>

<success_criteria>
- TOP-01: getTopDogs() called server-side in HistoryPage
- TOP-02: GET /api/stats confirmed working via curl
- TOP-03: /history page renders "Top Dogs" section above HistoryList with image, dogId, count per card
- TOP-04: Design system tokens visible in rendered page (orange primary, red dislike, rounded cards, correct font)
- D-01: getTopDogs() called in server component, result passed as prop to TopDogsSection
- No regressions to existing history page functionality
</success_criteria>

<output>
After completion, create `.planning/phases/05-top-dogs/05-04-SUMMARY.md`
</output>
