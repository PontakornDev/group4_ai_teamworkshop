---
phase: 05-top-dogs
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - components/TopDogsSection.tsx
autonomous: true
requirements:
  - TOP-03
  - TOP-04

must_haves:
  truths:
    - "TopDogsSection renders a 2-column grid with Most Liked and Most Disliked cards when both are present"
    - "When only one category has data, only that card renders — no placeholder"
    - "When both are null, empty state message renders inside the section (section is not hidden)"
    - "Cards use rounded-[24px], shadow-[0_4px_24px_rgba(0,0,0,0.04)], bg-surface-container-lowest"
    - "Section heading 'Top Dogs' uses text-headline-md text-on-surface"
    - "Most Liked label and count use text-primary; Most Disliked label and count use text-error"
    - "Dog image is 140px height, full-width, object-cover via Next.js Image"
    - "Dog ID displays as #{dogId.slice(0, 8)}"
    - "Count badge: number in text-headline-md, unit in text-body-md text-on-surface-variant"
  artifacts:
    - path: "components/TopDogsSection.tsx"
      provides: "TopDogsSection React component"
      exports: ["default TopDogsSection"]
  key_links:
    - from: "components/TopDogsSection.tsx"
      to: "TopDogsResult type from lib/storage.ts"
      via: "import type"
      pattern: "TopDogsResult"
---

## Phase Goal

**As a** dog-loving user, **I want to** see which dogs got the most likes and dislikes, **so that** I know which pups are crowd favorites and which are underdogs.

<objective>
Build the `TopDogsSection` component that renders the Top Dogs leaderboard UI following the design contract in `05-UI-SPEC.md` exactly.

Purpose: This is the visible user-facing deliverable. After this plan, the component exists and is verifiable in isolation before being wired into the history page.
Output: `components/TopDogsSection.tsx` — a self-contained client-renderable server component.
</objective>

<execution_context>
@/Users/k6b6/.claude/get-shit-done/workflows/execute-plan.md
@/Users/k6b6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/CLAUDE.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/05-top-dogs/05-UI-SPEC.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/05-top-dogs/05-01-SUMMARY.md

<interfaces>
<!-- Props contract — from 05-UI-SPEC.md Component Inventory -->
interface TopDogsProps {
  topDogs: {
    mostLiked: { dogId: string; imageUrl: string; likeCount: number } | null;
    mostDisliked: { dogId: string; imageUrl: string; dislikeCount: number } | null;
  } | null;
}

<!-- TopDogsResult from lib/storage.ts (after Plan 01) -->
export type TopDogsResult = {
  mostLiked: { dogId: string; imageUrl: string; likeCount: number } | null;
  mostDisliked: { dogId: string; imageUrl: string; dislikeCount: number } | null;
};

<!-- Next.js Image usage pattern from existing components -->
import Image from "next/image";
// For images from random.dog (external domain), use unoptimized={true} or configure remotePatterns.
// Existing project uses unoptimized on dog images — follow same pattern.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build TopDogsSection component</name>
  <files>components/TopDogsSection.tsx</files>
  <behavior>
    - Renders section heading "Top Dogs" with text-headline-md text-on-surface
    - When mostLiked and mostDisliked both present: renders grid grid-cols-2 gap-md with two cards
    - When mostLiked only: renders single card in grid (no placeholder for missing mostDisliked)
    - When mostDisliked only: renders single card in grid (no placeholder for missing mostLiked)
    - When both null (or topDogs is null): renders empty state — heading "No top dogs yet", body "No swipes yet — start swiping to see top dogs!"
    - Most Liked card label "Most Liked" uses text-primary; count number uses text-primary
    - Most Disliked card label "Most Disliked" uses text-error; count number uses text-error
    - Dog ID shown as #{dogId.slice(0, 8)} in text-label-lg text-on-surface-variant
    - Like count: `<span className="text-headline-md font-bold text-primary">{likeCount}</span><span className="text-body-md text-on-surface-variant"> likes</span>`
    - Dislike count: same pattern with text-error and " dislikes"
    - Card container: bg-surface-container-lowest rounded-[24px] shadow-[0_4px_24px_rgba(0,0,0,0.04)] overflow-hidden flex flex-col
    - Image: relative w-full h-[140px], Next.js Image fill object-cover unoptimized
    - Info strip: p-sm flex flex-col gap-xs
    - Section outer wrapper: mb-lg
  </behavior>
  <action>
    Create `components/TopDogsSection.tsx` as a React server component (no "use client" directive needed — no event handlers or hooks).

    Imports needed:
    - `import Image from "next/image"`
    - `import type { TopDogsResult } from "@/lib/storage"`

    Props: Accept `{ topDogs: TopDogsResult | null }` — use the imported type, not a locally redefined interface.

    Structure the component as follows:

    1. Outer wrapper div with `mb-lg`
    2. Section heading: `<h3 className="text-headline-md font-bold text-on-surface mb-md">Top Dogs</h3>`
    3. Determine which cards to show: build an array of card configs from topDogs (filter nulls)
    4. If cards array is empty: render empty state div with heading "No top dogs yet" (text-headline-md text-on-surface) and body paragraph "No swipes yet — start swiping to see top dogs!" (text-body-md text-on-surface-variant)
    5. If cards array is non-empty: render `<div className="grid grid-cols-2 gap-md">` containing only the non-null cards

    Each dog card:
    - Container: `<div className="bg-surface-container-lowest rounded-[24px] shadow-[0_4px_24px_rgba(0,0,0,0.04)] overflow-hidden flex flex-col">`
    - Image wrapper: `<div className="relative w-full h-[140px]">` containing `<Image src={imageUrl} alt={label} fill className="object-cover" unoptimized />`
    - Info strip: `<div className="p-sm flex flex-col gap-xs">`
      - Label: `<span className="text-label-lg font-semibold {labelColor}">{label}</span>` where labelColor is `text-primary` for Most Liked, `text-error` for Most Disliked
      - Dog ID: `<span className="text-label-lg text-on-surface-variant">#{dogId.slice(0, 8)}</span>`
      - Count: inline span pair as specified in behavior block above

    Do NOT add hover effects, click handlers, or transitions — cards are display-only per UI-SPEC Interaction Contract.
    Do NOT use "use client". Do NOT add loading states — parent server component handles data fetching.
  </action>
  <verify>
    <automated>cd /Users/k6b6/Desktop/claude/group4_ai_teamworkshop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `components/TopDogsSection.tsx` exists and exports a default React component
    - TypeScript compiles with no errors
    - Component accepts TopDogsResult prop and renders all three states: both cards, partial, empty
    - Design tokens match UI-SPEC exactly: rounded-[24px], shadow value, bg-surface-container-lowest, text-primary / text-error semantic split, text-headline-md count, image h-[140px]
    - TOP-03 and TOP-04 satisfied
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| swipes.json → component | imageUrl values originate from random.dog API and are stored verbatim; rendered in Next.js Image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | TopDogsSection image src | accept | imageUrl comes from random.dog (trusted third party already in use by the project). dogId is a UUID substring — no PII. |
| T-05-06 | Tampering | TopDogsSection render | accept | Component is read-only display; no mutation. Props are server-computed values, not user-supplied strings injected into HTML attributes. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors
- Component file exists at `components/TopDogsSection.tsx`
- File does not contain "use client"
- File imports `TopDogsResult` from `@/lib/storage`
- All three render states present in component logic (both, partial, empty)
- Tailwind classes match: `rounded-[24px]`, `shadow-[0_4px_24px_rgba(0,0,0,0.04)]`, `bg-surface-container-lowest`, `text-primary`, `text-error`, `text-headline-md`, `h-[140px]`
</verification>

<success_criteria>
- TOP-03: Component renders "Top Dogs" section with dog image, dogId, and count per card
- TOP-04: Design tokens implemented — Quicksand (inherited from body), #9b4500 via text-primary, rounded-[24px], correct shadow
- D-03: No placeholder for missing category
- D-04: Empty state message shown when both null
- D-05: Vertical spotlight card with 140px image, label, truncated dogId, prominent count badge
- D-06: grid grid-cols-2 gap-md on all screen sizes
- D-07: "Top Dogs" heading in text-headline-md text-on-surface
- D-08: All shape/shadow/bg tokens correct
</success_criteria>

<output>
After completion, create `.planning/phases/05-top-dogs/05-03-SUMMARY.md`
</output>
