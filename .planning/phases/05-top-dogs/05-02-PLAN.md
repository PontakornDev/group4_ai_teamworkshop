---
phase: 05-top-dogs
plan: "02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - app/api/stats/route.ts
autonomous: true
requirements:
  - TOP-02

must_haves:
  truths:
    - "GET /api/stats returns 200 with JSON matching TopDogsResult shape"
    - "Endpoint requires no auth token — public access"
    - "Response body matches { mostLiked: {...}|null, mostDisliked: {...}|null }"
  artifacts:
    - path: "app/api/stats/route.ts"
      provides: "GET handler returning getTopDogs() result as JSON"
      exports: ["GET"]
  key_links:
    - from: "app/api/stats/route.ts GET"
      to: "lib/storage.ts getTopDogs"
      via: "direct import and await"
      pattern: "getTopDogs\\(\\)"
---

## Phase Goal

**As a** dog-loving user, **I want to** see which dogs got the most likes and dislikes, **so that** I know which pups are crowd favorites and which are underdogs.

<objective>
Create the `GET /api/stats` route that exposes `getTopDogs()` over HTTP with no auth guard.

Purpose: Provides a public data endpoint consumable by any client. The history page server component calls `getTopDogs()` directly, but the API route enables future client-side or external use.
Output: `app/api/stats/route.ts` with a named `GET` export.
</objective>

<execution_context>
@/Users/k6b6/.claude/get-shit-done/workflows/execute-plan.md
@/Users/k6b6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/CLAUDE.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/05-top-dogs/05-01-SUMMARY.md

<interfaces>
<!-- From lib/storage.ts after Plan 01 completes -->
export type TopDogsResult = {
  mostLiked: { dogId: string; imageUrl: string; likeCount: number } | null;
  mostDisliked: { dogId: string; imageUrl: string; dislikeCount: number } | null;
};
export async function getTopDogs(): Promise<TopDogsResult>

<!-- Existing App Router route pattern (from app/api/dog/route.ts) -->
import { NextResponse } from "next/server";
export async function GET(request: Request) {
  // ...
  return NextResponse.json(data);
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/api/stats/route.ts</name>
  <files>app/api/stats/route.ts</files>
  <behavior>
    - Test: GET /api/stats with no Authorization header → 200 response (no auth guard)
    - Test: response Content-Type is application/json
    - Test: response body is parseable JSON with keys "mostLiked" and "mostDisliked"
    - Test: when swipes.json is empty, both values are null
  </behavior>
  <action>
    Create `app/api/stats/route.ts` as a new file (parent directory `app/api/stats/` must be created).

    File contents:
    - Import `NextResponse` from `"next/server"`
    - Import `getTopDogs` from `"@/lib/storage"`
    - Export a named async `GET` function with no parameters (no `request` param needed — no query params used)
    - Body: `const result = await getTopDogs(); return NextResponse.json(result);`
    - No try/catch — let Next.js error boundary handle unexpected throws (consistent with other routes in this project)
    - No auth check — this endpoint is public per D-02

    Do not add any other exports. Do not add middleware. Do not add rate limiting — out of scope for Phase 5.
  </action>
  <verify>
    <automated>cd /Users/k6b6/Desktop/claude/group4_ai_teamworkshop && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `app/api/stats/route.ts` exists and exports `GET`
    - TypeScript compiles with no errors
    - TOP-02 satisfied: endpoint returns `getTopDogs()` JSON, no auth guard
    - Manual smoke test: `curl http://localhost:3000/api/stats` returns JSON with `mostLiked` and `mostDisliked` keys
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → /api/stats | Unauthenticated GET; no user-supplied input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | GET /api/stats | accept | Endpoint intentionally public per D-02. Exposes aggregated counts and dogIds only — no PII, no user data, no auth tokens. |
| T-05-04 | Denial of Service | GET /api/stats | accept | No user-controlled computation; reads a local JSON file. Rate limiting is out of scope for this prototype. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with no new errors
- File `app/api/stats/route.ts` exists and contains a named `GET` export
- No auth import or session check present in the route file
- `curl -s http://localhost:3000/api/stats | jq 'keys'` returns `["mostDisliked","mostLiked"]`
</verification>

<success_criteria>
- TOP-02: GET /api/stats returns `getTopDogs()` result as JSON with no auth requirement
- Route file created in correct App Router location
- No TypeScript errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05-top-dogs/05-02-SUMMARY.md`
</output>
