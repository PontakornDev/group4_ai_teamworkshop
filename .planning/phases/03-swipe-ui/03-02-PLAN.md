---
phase: 03-swipe-ui
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "The /swipe page shows a dog image in a card layout"
    - "Clicking Like or Dislike sends a POST to /api/swipe with correct fields"
    - "After each swipe the next dog image loads automatically"
    - "The navbar shows the Google avatar and display name"
    - "Swipe records appear in swipes.json after actions are taken"
  artifacts:
    - path: "app/api/dog/route.ts"
      provides: "Dog API with imageUrl field (fixed in plan 01)"
      contains: "imageUrl:"
    - path: "data/swipes.json"
      provides: "Persistent swipe record storage"
  key_links:
    - from: "GET /api/dog?username="
      to: "SwipeClient.tsx DogData"
      via: "JSON response { imageUrl, dogId }"
      pattern: "imageUrl"
    - from: "SwipeClient swipe()"
      to: "POST /api/swipe"
      via: "fetch with body { dogId, imageUrl, username, email, action }"
      pattern: "api/swipe"
    - from: "POST /api/swipe"
      to: "data/swipes.json"
      via: "appendAction() in lib/storage.ts"
      pattern: "appendAction"
---

<objective>
Verify the full swipe loop end-to-end: dog loads on /swipe, Like/Dislike buttons post the action correctly, swipes.json records the entry, and the next dog loads. This plan contains no code changes — it confirms that the bug fix in plan 01 restored correct behavior across all five success criteria.

Purpose: Prove the phase is shippable. The verify-only checkpoint ensures no regression before marking Phase 3 complete.
Output: Human-confirmed swipe loop working; dev server log and swipes.json entry as evidence.
</objective>

<execution_context>
@/Users/k6b6/.claude/get-shit-done/workflows/execute-plan.md
@/Users/k6b6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/03-swipe-ui/03-CONTEXT.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/03-swipe-ui/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight checks</name>
  <files></files>

  <read_first>
    app/api/dog/route.ts — confirm imageUrl fix from plan 01 is present before running checks.
    data/swipes.json — read current state (may not exist yet; that is fine).
  </read_first>

  <action>
    Run the following automated checks in sequence. Report the output of each.

    **Check A — API field name is correct:**
    ```bash
    grep -n "imageUrl:" app/api/dog/route.ts
    ```
    Expected: 2 lines. If 0, plan 01 was not applied — stop and re-run plan 01.

    **Check B — TypeScript compiles without errors:**
    ```bash
    npx tsc --noEmit 2>&1
    ```
    Expected: exits 0, no output. If errors exist, fix them before proceeding.

    **Check C — Dev server responds (start if not running):**
    ```bash
    curl -s "http://localhost:3000/api/dog?username=testuser" | head -c 200
    ```
    Expected JSON shape: `{"imageUrl":"https://random.dog/...","dogId":"..."}`.
    The key MUST be `imageUrl`, not `url`.
    If server is not running, start it with `npm run dev` in the background before running curl.

    **Check D — Swipe POST round-trip:**
    Extract `imageUrl` and `dogId` from the Check C response, then POST a swipe:
    ```bash
    curl -s -X POST http://localhost:3000/api/swipe \
      -H "Content-Type: application/json" \
      -d '{"dogId":"<dogId-from-check-C>","imageUrl":"<imageUrl-from-check-C>","username":"testuser","email":"test@example.com","action":"like"}'
    ```
    Expected: HTTP 200, response body `{"ok":true}` or similar success indicator.

    **Check E — swipes.json updated:**
    ```bash
    cat data/swipes.json
    ```
    Expected: JSON array containing a record with `"dogId"` matching the value from Check C, with a `col` entry where `username` is `"testuser"` and `action` is `"like"`.
  </action>

  <verify>
    <automated>grep -c "imageUrl:" /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/app/api/dog/route.ts</automated>
    Expected: 2

    <automated>curl -s "http://localhost:3000/api/dog?username=preflight" | python3 -c "import sys,json; d=json.load(sys.stdin); assert 'imageUrl' in d, f'Missing imageUrl key, got: {list(d.keys())}'; print('OK: imageUrl present')"</automated>
    Expected output: `OK: imageUrl present`
  </verify>

  <acceptance_criteria>
    - `grep -c "imageUrl:" app/api/dog/route.ts` returns `2`
    - `npx tsc --noEmit` exits 0
    - `curl /api/dog?username=preflight` returns JSON with key `imageUrl` (not `url`)
    - `curl POST /api/swipe` returns a 2xx response
    - `data/swipes.json` contains the posted record with correct `dogId`, `username`, `action`, and an ISO `timestamp`
  </acceptance_criteria>

  <done>All five automated checks pass with no manual intervention required.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end swipe loop verification</name>

  <read_first>
    No files to read — this is a manual browser verification step.
  </read_first>

  <what-built>
    Plan 01 fixed the url→imageUrl field name bug in app/api/dog/route.ts. Task 1 confirmed the fix via curl. This checkpoint verifies the full browser experience: auth guard, dog card render, swipe action, next dog auto-load, and navbar display.
  </what-built>

  <how-to-verify>
    With `npm run dev` running (http://localhost:3000):

    **Step 1 — Auth guard:**
    Visit http://localhost:3000 without signing in.
    Expected: redirected to /login (not /swipe).

    **Step 2 — Sign in:**
    Click "Sign in with Google" on /login. Complete the Google OAuth flow.
    Expected: redirected to /swipe after authentication.

    **Step 3 — Dog card loads:**
    Confirm a dog image appears in the card on /swipe.
    Expected: real dog photo visible (not broken image, not a spinner stuck forever).

    **Step 4 — Navbar:**
    Confirm the top/sidebar navbar shows your Google avatar image and display name.
    Expected: avatar circle with your Google profile photo + your Google account name.

    **Step 5 — Like action:**
    Click the orange Like button (large, rightmost FAB).
    Expected: current dog disappears and the next dog loads in the card automatically. No page reload.

    **Step 6 — Dislike action:**
    Click the red Dislike button (close icon, leftmost FAB).
    Expected: same — current dog disappears and next dog loads.

    **Step 7 — swipes.json persisted:**
    After at least one swipe, check the file on disk:
    ```bash
    cat data/swipes.json
    ```
    Expected: JSON array with records containing your Google display name as `username`, your Google email as `email`, and the correct `action` value.
  </how-to-verify>

  <acceptance_criteria>
    - Dog image renders in card (no broken img src, no infinite spinner)
    - Like button triggers next dog load (without page reload)
    - Dislike button triggers next dog load (without page reload)
    - Navbar shows Google avatar + display name (not empty, not placeholder)
    - data/swipes.json contains entries with real username, email, action, and ISO timestamp after swipes
  </acceptance_criteria>

  <resume-signal>
    Type "approved" if all five acceptance criteria pass.
    If any step fails, describe which step and what you observed — the executor will investigate and fix.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /swipe | NextAuth session required; no session redirects to /login |
| browser → POST /api/swipe | Body fields trusted from authenticated session context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | POST /api/swipe username/email fields | accept | MVP scope — no server-side session validation on POST body. Username/email come from client. Acceptable for prototype with no adversarial users. |
| T-03-04 | Denial of Service | GET /api/dog random.dog upstream | accept | MAX_ATTEMPTS=5 caps retry loop. Upstream failure returns 500 with error message — no infinite loop. |
</threat_model>

<verification>
Phase 3 is complete when:

1. `grep -c "imageUrl:" app/api/dog/route.ts` returns `2`
2. `npx tsc --noEmit` exits 0
3. `curl "http://localhost:3000/api/dog?username=test"` returns `{"imageUrl":"...","dogId":"..."}`
4. Human checkpoint approved (all five browser steps pass)
5. `data/swipes.json` contains real swipe records from the browser session
</verification>

<success_criteria>
All five ROADMAP Phase 3 success criteria are confirmed true:
1. /swipe page shows a dog image in a card layout — verified in browser (Step 3)
2. User can click Like or Dislike buttons — verified in browser (Steps 5, 6)
3. Each click sends POST /api/swipe with correct fields — verified via swipes.json (Step 7)
4. After each swipe, the next dog loads automatically — verified in browser (Steps 5, 6)
5. Navbar displays Google avatar + display name — verified in browser (Step 4)
</success_criteria>

<output>
After completion, create `.planning/phases/03-swipe-ui/03-02-SUMMARY.md` using the summary template.
</output>
