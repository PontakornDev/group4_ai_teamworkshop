---
phase: 03-swipe-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/dog/route.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04

must_haves:
  truths:
    - "GET /api/dog returns a JSON object with an `imageUrl` key (not `url`)"
    - "GET /api/dog returns a `dogId` key alongside `imageUrl` in both code paths"
  artifacts:
    - path: "app/api/dog/route.ts"
      provides: "Dog API route returning correctly named fields"
      contains: "imageUrl:"
  key_links:
    - from: "app/api/dog/route.ts"
      to: "app/swipe/SwipeClient.tsx DogData interface"
      via: "JSON response key `imageUrl`"
      pattern: "imageUrl:"
---

<objective>
Fix the field name mismatch in the dog API route: the route returns `url:` in both response paths, but every downstream consumer (`DogData` interface in `SwipeClient.tsx`, `SwipeCard` props, the swipe POST body) expects `imageUrl:`. Rename the two `url:` keys to `imageUrl:`.

Purpose: Without this fix the client receives `{ url: ..., dogId: ... }`, `dog.imageUrl` is `undefined`, the card renders a broken image, and the swipe POST sends `imageUrl: undefined` — breaking the full swipe loop.
Output: `app/api/dog/route.ts` with both NextResponse.json() calls returning `imageUrl:`.
</objective>

<execution_context>
@/Users/k6b6/.claude/get-shit-done/workflows/execute-plan.md
@/Users/k6b6/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/ROADMAP.md
@/Users/k6b6/Desktop/claude/group4_ai_teamworkshop/.planning/phases/03-swipe-ui/03-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. -->

From app/swipe/SwipeClient.tsx (lines 7-10):
```typescript
interface DogData {
  dogId: string;
  imageUrl: string;   // <-- this is what the API response MUST contain
}
```

From components/SwipeCard.tsx (props):
```typescript
// SwipeCard accepts: { imageUrl: string, dogId: string }
```

From app/swipe/SwipeClient.tsx swipe() body (line 45):
```typescript
body: JSON.stringify({ dogId: dog.dogId, imageUrl: dog.imageUrl, username, email, action })
// dog.imageUrl must be defined — comes from API response
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename `url:` to `imageUrl:` in both NextResponse.json() calls</name>
  <files>app/api/dog/route.ts</files>

  <read_first>
    app/api/dog/route.ts — read the full file before editing; understand both return paths.
  </read_first>

  <action>
    Edit `app/api/dog/route.ts`. Make exactly two key renames — nothing else changes:

    **Change 1 — unseen dog return (line 22):**
    Before: `return NextResponse.json({ url: unseen.imageUrl, dogId: unseen.dogId });`
    After:  `return NextResponse.json({ imageUrl: unseen.imageUrl, dogId: unseen.dogId });`

    **Change 2 — random.dog fetch return (line 31):**
    Before: `return NextResponse.json({ url: data.url, dogId: extractDogId(data.url) });`
    After:  `return NextResponse.json({ imageUrl: data.url, dogId: extractDogId(data.url) });`

    Do not change any other code — function signatures, imports, helper functions, error response, and MAX_ATTEMPTS loop are all correct and must not be touched.
  </action>

  <verify>
    <automated>grep -n "imageUrl:" /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/app/api/dog/route.ts</automated>
    Expected: two lines — one containing `imageUrl: unseen.imageUrl` and one containing `imageUrl: data.url`.

    <automated>grep -v '^//' /Users/k6b6/Desktop/claude/group4_ai_teamworkshop/app/api/dog/route.ts | grep -c "url: unseen\|url: data\.url"</automated>
    Expected output: 0 (no remaining bare `url:` field assignments in response objects).
  </verify>

  <acceptance_criteria>
    - `grep -n "imageUrl:" app/api/dog/route.ts` returns exactly 2 matches
    - `grep -n "url: unseen.imageUrl\|url: data.url" app/api/dog/route.ts` returns 0 matches
    - File still compiles: `npx tsc --noEmit` exits 0 (or `next build` does not emit TS errors for this file)
    - The error response line (`{ error: ... }`) is unchanged
  </acceptance_criteria>

  <done>Both NextResponse.json() success responses in app/api/dog/route.ts use the key `imageUrl:` instead of `url:`. No other code is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/dog | Username supplied as query param; unauthenticated access possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | GET /api/dog?username= | accept | Username is display-only; no privilege escalation possible from spoofing it. Real identity enforced by NextAuth session on the page. |
| T-03-02 | Information Disclosure | GET /api/dog response | accept | Returns only public dog image URLs and dogIds — no PII in response. |
</threat_model>

<verification>
After task completes:

```bash
grep -n "imageUrl:" app/api/dog/route.ts
# Must show 2 lines

grep -n "url: unseen\|url: data" app/api/dog/route.ts
# Must show 0 lines

npx tsc --noEmit 2>&1 | grep "dog/route"
# Must show no errors
```
</verification>

<success_criteria>
- `app/api/dog/route.ts` returns `{ imageUrl, dogId }` in both success code paths
- Zero TypeScript errors in the file
- No other files are modified
</success_criteria>

<output>
After completion, create `.planning/phases/03-swipe-ui/03-01-SUMMARY.md` using the summary template.
</output>
